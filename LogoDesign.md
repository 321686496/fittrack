# FitTrack 小程序图标设计方案（活力运动风）
**文档版本**：v1.0  
**设计日期**：2026-05-26  
**产品定位**：单机版健身训练计划与记录工具  
**设计风格**：活力运动风（年轻化、动感、高辨识度）  
**适配平台**：微信小程序（全机型兼容）

---

## 一、设计概述
### 1.1 设计目标
为FitTrack打造符合产品核心功能（训练计划、执行记录、数据统计）的视觉标识，突出**运动活力、年轻向上**的品牌调性，在微信小程序列表中具备高辨识度，同时适配小尺寸展示场景，保证清晰可辨。

### 1.2 核心设计理念
- **动感优先**：通过渐变、线条、几何元素传递运动的力量感与节奏感
- **功能关联**：图形元素直接呼应「训练计时、动作执行、数据追踪」三大核心功能
- **轻量化**：扁平化设计，无复杂细节，适配小程序图标加载与展示规范
- **品牌统一**：色彩与图形可延伸至APP内UI组件，形成完整视觉体系

---

## 二、核心设计规范
### 2.1 色彩系统
| 色彩类型 | 色值 | 使用场景 | 备注 |
|---------|------|---------|------|
| 主色调（活力橙） | `#FF6B35` | 图标主背景、选中态文字、按钮主色 | 运动类产品经典色，传递热情与能量 |
| 渐变辅助色1 | `#FF8C42` | 图标背景渐变上层 | 增强层次感与动感 |
| 渐变辅助色2 | `#FF5722` | 图标背景渐变下层 | 加深视觉冲击力 |
| 文字色 | `#FFFFFF` | 图标内文字、图形线条 | 高对比度，保证可读性 |
| 点缀色 | `#222222` | 图形细节描边（可选） | 增强图形轮廓清晰度 |
| 中性底色 | `#F5F5F5` | 单色版本图标背景 | 适配浅色背景场景 |

### 2.2 主图标设计（108×108px 微信小程序标准尺寸）
#### 2.2.1 整体结构
- **外轮廓**：圆角正方形，圆角半径 `16px`（严格遵循微信小程序图标规范）
- **背景**：线性渐变，角度 `135°`，从 `#FF8C42` 过渡到 `#FF5722`
- **内边距**：上下左右各 `12px`，保证核心元素不被裁切
- **整体比例**：图形元素占比 60%，文字元素占比 30%，留白占比 10%

#### 2.2.2 核心图形元素（居中展示）
由「计时器+运动轨迹」组合而成，直接呼应产品核心功能：
1. **计时器主体**：圆形轮廓，白色线条绘制，线条粗细 `3px`，直径 `48px`
2. **指针设计**：简化为单根指针，指向 10 点方向（模拟训练计时状态），白色填充
3. **运动轨迹**：计时器右侧添加 3 条倾斜折线（角度 45°），白色线条，长度依次为 `12px`、`16px`、`20px`，模拟运动轨迹与速度感
4. **细节处理**：计时器中心添加一个直径 `4px` 的白色圆点，增强精致度

#### 2.2.3 文字排版（图形下方居中）
- **文字内容**：`FitTrack`
- **字体选择**：无衬线粗体（推荐：思源黑体 Bold / 阿里巴巴普惠体 Bold）
- **字号**：`14px`（108×108px 尺寸下）
- **字间距**：`1px`，增强可读性
- **对齐方式**：水平居中，与图形底部间距 `8px`
- **颜色**：纯白色 `#FFFFFF`

#### 2.2.4 图标结构示意图
```
┌─────────────────────────────┐
│  渐变背景（#FF8C42→#FF5722）│
│                             │
│        ┌─────────┐          │
│        │  计时器  │          │
│        │  + 轨迹  │          │
│        └─────────┘          │
│                             │
│         FitTrack            │
│                             │
└─────────────────────────────┘
```

### 2.3 图标变体设计
#### 2.3.1 单色版本（适配深色背景/打印场景）
- 背景：纯白色 `#FFFFFF`
- 图形与文字：主色调 `#FF6B35`
- 适用场景：分享卡片、打印物料、深色模式页面

#### 2.3.2 深色模式适配版本
- 背景：深灰色 `#1E1E1E`
- 图形与文字：主色调 `#FF6B35`
- 适用场景：APP内深色模式下的logo展示

#### 2.3.3 极简图形版本（小尺寸场景）
- 仅保留「计时器+运动轨迹」核心图形
- 去掉文字元素
- 适用场景：搜索结果图标、分享缩略图、Tab栏图标

---

## 三、多尺寸适配规范
### 3.1 微信小程序全场景尺寸
| 展示场景 | 尺寸（px） | 设计要求 |
|---------|-----------|---------|
| 桌面主图标 | 108×108 | 完整版本（图形+文字） |
| 搜索结果图标 | 64×64 | 完整版本（文字字号缩小至 10px） |
| 分享卡片图标 | 84×84 | 完整版本 |
| 个人中心logo | 128×128 | 完整版本（可适当放大图形） |
| 启动页logo | 256×256 | 完整版本（背景透明） |
| Tab栏图标 | 24×24 | 极简图形版本 |

### 3.2 小尺寸优化规则
- 当尺寸 ≤ 64×64px 时，将文字字间距调整为 `0.5px`，线条粗细调整为 `2px`
- 当尺寸 ≤ 32×32px 时，**强制使用极简图形版本**，去掉所有文字元素
- 所有尺寸下，圆角半径按比例缩放，保持视觉一致性

---

## 四、应用场景与使用规范
### 4.1 推荐使用场景
1. **微信小程序主图标**：使用 108×108px 完整版本
2. **APP内启动页**：使用 256×256px 透明背景版本
3. **分享卡片/海报**：使用 84×84px 完整版本或单色版本
4. **页面内品牌展示**：个人中心、关于页面使用 128×128px 版本
5. **Tab栏图标**：使用 24×24px 极简图形版本，选中态为 `#FF6B35`，未选中态为 `#999999`

### 4.2 禁用规范
- ❌ 禁止拉伸或压缩图标，保持宽高比 1:1
- ❌ 禁止修改图标色彩、渐变方向与文字内容
- ❌ 禁止在图标上添加额外边框、阴影或装饰元素
- ❌ 禁止将图标与其他图形拼接使用
- ❌ 禁止在深色背景上使用彩色渐变版本（应使用深色模式适配版本）

---

## 五、落地实现指南
### 5.1 图标导出规范
| 导出格式 | 尺寸 | 用途 | 备注 |
|---------|------|------|------|
| PNG（透明底） | 108×108、64×64、84×84、128×128、256×256 | 小程序图标、页面内展示 | 分辨率 72dpi，无压缩 |
| SVG（矢量） | 任意 | 开发复用、后期修改 | 保证图形可无限缩放不失真 |
| PNG（白底） | 108×108 | 小程序审核提交 | 部分审核场景要求白底图标 |

### 5.2 小程序代码集成示例
#### 5.2.1 全局配置（app.json）
```json
{
  "window": {
    "navigationBarBackgroundColor": "#FF6B35",
    "navigationBarTitleText": "FitTrack",
    "navigationBarTextStyle": "white"
  },
  "tabBar": {
    "color": "#999999",
    "selectedColor": "#FF6B35",
    "backgroundColor": "#ffffff",
    "list": [
      {
        "pagePath": "pages/index/index",
        "text": "首页",
        "iconPath": "assets/images/icons/home.png",
        "selectedIconPath": "assets/images/icons/home-active.png"
      },
      {
        "pagePath": "pages/plan/plan",
        "text": "计划",
        "iconPath": "assets/images/icons/plan.png",
        "selectedIconPath": "assets/images/icons/plan-active.png"
      }
    ]
  }
}
```

#### 5.2.2 启动页logo使用（pages/index/index.wxml）
```xml
<view class="launch-logo">
  <image 
    src="/assets/images/logo-256.png" 
    mode="widthFix" 
    style="width: 120rpx; height: 120rpx;"
  />
  <text class="logo-text">FitTrack</text>
</view>
```

### 5.3 与现有UI体系适配
- 将原代码中导航栏主色 `#4A90E2` 统一替换为 `#FF6B35`，保持品牌色一致
- 按钮、选中态、强调文字均使用主色调 `#FF6B35`
- 图表库（wx-charts）主色同步调整为 `#FF6B35`，保证数据可视化风格统一

---

## 六、验收标准
1. ✅ 108×108px 尺寸下，图形与文字清晰可辨，无模糊
2. ✅ 渐变过渡自然，无明显色块断层
3. ✅ 所有小尺寸版本（≥32×32px）均可识别核心图形
4. ✅ 微信开发者工具中预览无裁切、无变形
5. ✅ 深色模式下适配版本显示正常
6. ✅ 与APP内UI组件色彩风格统一

---

## 七、设计源文件说明
- 源文件格式：Figma / Sketch
- 包含内容：所有尺寸图标、色彩色板、字体文件、应用场景示例
- 可修改项：文字内容、色彩数值（需保持整体风格统一）
- 不可修改项：图形比例、圆角半径、核心元素结构